export const enhanceDescriptionWithAI = async (text) => {
  return `There has been a reported issue regarding the following matter:

${text}

The issue has caused inconvenience to the affected individuals and requires prompt attention from the concerned authorities to prevent further impact.`;
};
